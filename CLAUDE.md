# このリポジトリで作業するときの約束

## 変更したら、そのたびにデプロイする

`main` に push すると GitHub Actions が GitHub Pages へ出す
（`.github/workflows/pages.yml`）。**ブランチへの push ではデプロイされない。**

作業ブランチにコミットしたら `main` へ取り込んで push するところまでを1回分とする。
聞き直さなくてよい。

反映されたかは、画面右下の版表示（commit hash と日本時間）で確かめる。
version.json はデプロイのたびにワークフローが書き出すので、手で更新しない。

## 最初の画面の精度は、言われたときだけ確かめる

`src/app.js` の `rounds-choices` に「20問 80%」のような数字を直接書いている。
これは同梱プールでの実測値で、次のどれかを変えると動く。

- `data/faces.json`（顔を足す・外す）
- `src/calibration.js`（較正の係数）
- `src/model.js`（ペアの選び方、重み、層の切り方）
- `src/features.js`（測る項目）

ただし **確かめるのは頼まれたときだけでよい。** 勝手に走らせない。
1回に数分かかるので、そのぶん会話が重くなる。

```
npm run acc-check
```

頼まれて走らせ、ずれていれば実測の値に書き換える。

いまの数字（20問 79% / 30問 82% / 45問 85% / 90問 89%）は
**241枚で実測ずみ**（仮想ユーザー400人、4つとも ±0.1ポイント以内）。いまは245枚。

プールを 305 → 271 → 278 → 291 → 312 → 231 → 241 → 245 と大きく動かしても、
この4つは動かなかった。**枚数を変えただけでは予測一致率はほとんど動かない**
ので、顔を足し引きしただけなら測り直しは急がなくてよい。
較正の係数・ペアの選び方・測る項目を変えたときは測ること。

## 同一人物のしきい値は目で決める

`tools/find-duplicates.mjs` が出す「重複の候補」はプール自身の下位2%という
相対基準なので、どれだけきれいにしても必ず約2%を挙げ続ける。読むのは
「近い順に並べたもの」としてで、外す線は自分で決める。

**いまの線は見た目の距離 0.22。** 距離の分布に自然な切れ目が無いので、
機械的には決められない。近い組を並べて本人に見せ、別人に見え始める距離を
決めてもらうこと（0.20 では「同一人物に見える」と判定された）。

連結成分でまとめてはいけない。a≈b, b≈c で a と c も同組になり、別人を
巻き込む。「残った顔どうしが必ずしきい値より離れている」状態を作る。

## 測定ツールは種を渡して呼ぶ

`newStats()` を引数なしで呼ぶと、顔ごとの贔屓（`src/model.js` の `luckOf`）が
`Math.random` に落ちて、**同じ種・同じプールでも毎回ちがう結果**になる。
`feature-report` / `simulate` / `calibrate` の3つがこれをやっていて、
README の「項目ごとの出やすさ」の表と「同一人物を外すと偏りは悪くなる」は
この状態で測られた誤りだった（どちらも直した）。

いまは3つとも `newStats(rand)` を渡してある。**新しく測るものを足すときも
必ず種を渡すこと。**アプリ側（`src/app.js`）は毎回ちがう顔が出てほしいので
既定の `Math.random` のままでよい。

測り直したかどうかは、**同じコマンドを2回走らせて同じ値が出るか**で分かる。

なお `src/calibration.js` のいまの係数は、この修正より前に求めたもの。
求め直していないが、係数自体は400人の平均なので大きくは動かないはず。

## 較正をやり直したとき

`node tools/calibrate.mjs` が出す「後」の数字は当てにしない。
係数を求めたのと同じサンプルで効果を報告しているので過学習している
（満額で 1.02倍 と出ても、別の乱数で測ると 1.8倍 だったりする）。
強さを選ぶときは必ず別の乱数で測り直す。

## 次にデータセットを改良するとき

**`gemini-3-pro-image` を `--vibe idol-strong` で使う。**（8枚試して確かめた）

フラッシュ系4つは出尽くしている。8枚作っても 0.22 を越えて残るのは3〜5枚で、
残ったものは相関を悪化させる。**pro はまだ出尽くしていない。**

| モデル | 8枚中の残り | プールとの平均距離 | 状態 |
|---|---|---|---|
| `gemini-3.1-flash-image`（既定） | – | – | 出尽くし |
| `gemini-3.1-flash-image-preview` | 3枚 | 0.219 | 既定とほぼ同じ挙動 |
| `gemini-2.5-flash-image` | 2〜3枚 | – | 出尽くし |
| `gemini-3.1-flash-lite-image` | 4枚 | 0.220 | 10枚取り込んだ時点で飽和 |
| **`gemini-3-pro-image`** | **6枚** | **0.32〜0.40** | **まだ出る。次もこれ** |

プール自身の中央値が 0.36 なので、**pro の顔はプールの住人どうしと同じだけ
離れている。**フラッシュ系はしきい値 0.22 のすぐ上に張り付いていた。

**`--vibe idol` は pro には効かない。** 証明写真のような「実在感のある普通の人」に
なり、1回目の4枚は3枚が不採用だった。`--vibe idol-strong`（かわいさを職業として
名指しし、`FRAMING` の `natural everyday makeup` を後ろから上書きする）に
替えると、2回目は3枚とも採用になった。**`--soft` は付けない**
（末尾の念押しが外れる。1回目の失敗はこれも効いていた）。

```bash
npm run generate -- --decorrelate --pair noseWidth,mouthWidth \
  --model gemini-3-pro-image --vibe idol-strong --hair long \
  --female-ratio 1 --count 8 --budget-jpy <上限>
```

**かわいさを押しても散り方は落ちなかった。** README の「邪魔をしていたのは
かわいさの押さえ」はフラッシュ系の話で、pro では成り立たない。

**pro の単価はまだ分かっていない。** ただし `tools/generate.mjs` が使用トークン数を
出すようになったので、**1枚あたり出力 1283トークン**（1024×1024）は分かっている。
請求は遅れて反映されるのでその場では確かめられないが、後日の請求額と
この数字を突き合わせれば単価が出る。それまでは `--usd-per-image 0.25`
（40円/枚）のように多めに見積もって `--budget-jpy` を掛けること。
**4〜8枚から始めて、枚数を増やす前に必ず本人に相談すること。**

**相関への効きは、pro でも小さい。** 採用した4枚で `cute` 層 0.8077 → 0.8043。
このまま真っ直ぐ伸ばすと 0.80 → 0.60 には230枚ほど要る。**pro の値打ちは相関ではなく
「まだ作れること」のほう**だと思って使う。

**数枚の採否を、前後を別々に平均して決めないこと。** 4枚の差は600人でも見えず、
`node test/simulate.mjs 30 600 --pool` では「悪化」と出た（実際は誤差）。
同じ仮想ユーザーに両方のプールを解かせる `npm run ab-check` を1200人で使う。

```bash
git show HEAD:data/faces.json > .cache/faces-before.json
npm run ab-check -- .cache/faces-before.json data/faces.json 1200
```

`±` は2標準誤差。**またいでいるものは誤差の範囲**なので、符号だけを見ない。

判断の順番は、重複 → かわいさ → 効果。

1. **0.22 以下はプールにある顔の作り直し。** 候補どうしも見る（同じ生成で
   似た顔を繰り返す）。必要枚数の2倍を見積もること
2. **かわいさは必ず本人に見せて採否をもらう。** 短髪は cute 層に入れない。
   `hairLength` の実測は当てにならない（枠の外へ出た髪を拾えない）ので目で見る
3. **足す前に測る。** 相関（`cute` 層で見る）と `npm run pick`。
   残りやすさと相関への効きは逆向きになることがある（顔の型がそうだった）。
   生き残った枚数だけで決めない

`--ethnicity mixed` はプールとの平均距離 0.425 で壁を完全に抜けられるが、
**日本人だけのプールでなくなる。本人は「日本人のままにする」と判断済み。**

いまの目的は相関を切ること。それはそのままでよい。ただし、README の
「いつか作りたいもの → B専率」は非 `cute` 層（いま 245枚中43枚）が
増えないと成り立たない。**今回の作業ではないが、頼まれたときに
思い出せるように。**

## 画像を生成するとき

- **1枚あたり約12.5円。** 実測値（254枚で3,170円）。フラッシュ系での値。
  `--budget-jpy` の上限だけを頼りにすること。
- **請求は遅れて反映される。** 作った直後に残高を見ても変わっていないことがあり、
  後からまとめて引かれる。「減っていないから大丈夫」と判断しない。
  過去に、遅延中の差分から 1枚7.4円 と誤って見積もり、2,000円の予定で
  3,170円を使う事故を起こしている。
- 実在の人物に似せた顔は作らない。
- パーツ単位の指示（「目を離して」など）は効かない。
  かわいさの押さえを外せば効くが、顔の水準がプールから外れるので使えない。
  詳しくは `tools/generate.mjs` のコメントを参照。
